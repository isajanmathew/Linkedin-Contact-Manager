{
  "name": "linkedin-supabase-contact-saver",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node build-extension.js && vite build",
    "build:dev": "node build-extension.js && vite build --mode development",
    "build:extension": "node build-extension.js",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.0"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
